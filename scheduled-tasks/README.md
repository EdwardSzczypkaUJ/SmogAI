# Szablony Task Scheduler

Cztery pliki XML są przenośnymi szablonami diagnostycznymi z placeholderami. Główną metodą instalacji jest `scripts/Install-ScheduledTasks.ps1`, który wstawia rzeczywisty katalog projektu i runtime. Projekt nie wymaga `C:\SmogAI`.
